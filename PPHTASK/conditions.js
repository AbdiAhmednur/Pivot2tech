const age = 19;
const isOldEnough = age >= 18; /* Boolean value */

if (isOldEnough) {
  // This code will work only if a variable isOldEnough is true
  console.log("You can drive a car🚗");
}

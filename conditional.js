
// summary video & logic gates 

// gates 

// gate ||



var name = "abdirahman";

const gender = "male"

if (name.toLowerCase() ==="abdirahman" || gender.toLowerCase()=== "male"){
console.log("Hello " + name);
}else {
console.log ("sorry I do not know you");
}






// methods we are learning part of arrays is .toUpperCase. it is part of .slice .push and .pop
// there is a method to bring the whole word lower case which is .toLowerCase
// certain methods will have brackets like () a way you can write it is console.log (name.toLowerCase());
//.push() .pop() and .slice() all require brackets in the console.log 
// .toLowerCase and .toUpperCase are important in conditional JS 
// if and else if is used to manipulate the code 
// conditional if and else and else if 
// to say & in Javascript you have to use both &&
// the || gives Javascript an option to release an output 


// booleans are basically true or false 

//booleans are basically date types that are true or false
// methods have brackets for example toUpperCase() or toLowercase() and properties like length do not
// take advantage of || and &&
// sometimes it may be better to use switch cases rather than else if or if conditions 
// break; is so important as it creates a break; between the statements 
// break; will stop the loop


// these are methods 
naming()
.pop()
.push()
.toLowerCase()
.toUppercase()
.tostring()
.sort()